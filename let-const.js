//  break up with var
let balance = 1250;
balance = 1350;
let userName = 'tonni potash potash';
const weTogether = 'ami' + ' ' + userName;
console.log(weTogether);
// userName = 'moyna pakhi';

const numbers = [45, 35, 28];
//  numbers = [99, 123,44];
numbers.push(555);
numbers[1] = 333;
let sum = 0;
for (let i = 0; i < numbers.length; i++) {
    const number = numbers[1];
    console.log(number);
    sum = sum + number;
}

const student = { roll: 28, name: 'Tonni', job: 'student' };
student.name = 'tasnia';
// student = {name:'tasnia'};